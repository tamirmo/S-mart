<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@color/s_mart_ui_color</item>
        <item name="android:layoutDirection">ltr</item>
    </style>


    <style name="header" parent="android:Widget.TextView">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:layout_margin">10dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

    <style name="AnimatedButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/s_mart_titles_color</item>
        <item name="colorButtonNormal">@color/s_mart_ui_color</item>
    </style>

    <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#77ec20</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#FF0000
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#FF0000
        </item>
    </style>

</resources>
